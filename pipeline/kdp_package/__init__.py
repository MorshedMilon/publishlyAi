"""P15 KDP Package Builder — assemble a ready-to-upload KDP package (package only; a human uploads by
hand — this module NEVER uploads to KDP, has no API/browser client, and writes no listings row)."""
