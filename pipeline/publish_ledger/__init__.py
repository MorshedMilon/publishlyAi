"""P16 — Publish Ledger (minimal).

The single place a successful publish is recorded as a `listings` row. This MVP build covers the
auto-publish path P13 (Etsy) hands off to; the KDP human-confirmed path, the retire path, and any
querying/reconciliation are deferred to P16's full build slot.
"""
